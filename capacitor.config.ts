import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.erikbaksay.currencyconverter',
  appName: 'Currency Converter',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
